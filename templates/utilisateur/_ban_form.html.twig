<form method="post" action="{{ path('app_utilisateur_ban', {'idUser': utilisateur.idUser}) }}"
      onsubmit="return confirm('Are you sure you want to ban this item?');">
    <input type="hidden" class hidden name="_token" value="{{ csrf_token('ban' ~ utilisateur.idUser) }}">
    <button class="btn" {% if utilisateur.type == 'bannis' %} hidden disabled {% endif %} {% if utilisateur.type == 'admin' %} disabled {% endif %}>
        Ban
    </button>
</form>